# 独立提取验证记录

日期：2026-09-16。输入：SCEX 怀旧服现役更新脚本；原始文件哈希见 [source-snapshot.json](source-snapshot.json)。验证全部使用临时合成文件及 `127.0.0.1` 随机端口，没有连接生产更新服务或更改玩家客户端。

## PASS

- Windows、Python 3.14 环境：`python -m unittest discover -s tests -v`，5/5 通过，退出码 0；PowerShell 路径为 Windows PowerShell 5.1。
- 同一套测试指定 PowerShell 7.6.5，再次 5/5 通过，退出码 0。
- Python 与 PowerShell 同步器：旧模组更名更新、同路径资源替换、明确删除旧文件、原内容备份；额外模组、本地配置改动、本地主动删除、存档哨兵、缓存哨兵、游戏设置均保留。第二次运行文件正确且不额外产生备份。
- 独立发布器：从中性配置首次发布及递增版本发布、清理发布目录中的过期文件、核对清单中每个文件的 SHA-1/长度、生成更新摘要；使用配置文件测试数据，未访问 Modrinth。
- 下载服务：实际 HTTP GET/HEAD、错误令牌拒绝、目录列表拒绝、路径上跳拒绝、POST/PUT/DELETE 拒绝；移除的 `/kit/` 路由返回 404；文件替换后下载新内容。
- 服务启动脚本：配置目录、自定义令牌文件、默认本机监听参数传递。此项使用子程序替身，HTTP 行为由上一项覆盖。
- Windows 备用自刷新函数：缺失独立刷新脚本时使用的 `Save-DirectFile` 在本机 HTTP 源实际下载成功。提取时修复了原 BAT 内嵌 PowerShell 中的一个多余右括号；生产副本未改动。
- Python 语法、PowerShell 脚本语法、BAT 内嵌 PowerShell 语法及 BAT 的 CRLF 格式检查；`.bat/.ps1` 保留原始字节，不通过 Git 自动换行转换，保证 ZIP 下载中的 BAT 仍是 CRLF。
- 对发布目录扫描现役配置中的私有路径、地址及实际令牌，并检查常见凭据格式，未发现匹配。私有配置、更新地址文件、日志、整合包和玩家数据不纳入仓库。

## NOT_RUN

- macOS/Linux 实机、启动器 GUI 自动启动、Java/游戏启动与游戏运行中的后台预下载完整流程。
- Modrinth 官方源、公网下载、真实证书 TLS、反向代理、IPv6 与生产端口验收。
- 真实整包、多玩家并发、性能和长时间运行验收。

这些结果仅说明独立提取涉及的脚本路径和合成回归通过，不代表上述未运行环境已经验收。测试服务均已关闭，临时客户端及一次性测试目录已清理；源码和验证记录保留。
